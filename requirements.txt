# Guardian-X - Full Report Compliance
# Core
flask>=2.3.0
requests>=2.28.0
psutil>=5.9.0

# Edge Agent - Real-time file system monitoring
watchdog>=3.0.0

# Machine Learning (as per report: Isolation Forest, LightGBM, Autoencoder)
scikit-learn>=1.3.0
lightgbm>=4.0.0
numpy>=1.24.0
pandas>=2.0.0

# Explainable AI (SHAP, LIME - as per report)
shap>=0.43.0
lime>=0.2.0

# HuggingFace datasets for UNSW-NB15 (no manual download)
datasets>=2.14.0

# Autoencoder (report: TensorFlow/Keras) - optional, sklearn fallback if not installed
# tensorflow>=2.14.0
